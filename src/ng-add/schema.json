{
  "$schema": "http://json-schema.org/schema",
  "$id": "ds-builder-deploy-ng-add",
  "title": "Dominion Builder ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the target project"
    },
    "source": {
      "type": "string",
      "description": "Relative location of the file you want to move"
    },
    "destination": {
      "type": "string",
      "description": "Relative location where you would like to move the file"
    }
  },
  "required": ["project", "source", "destination"]
}
